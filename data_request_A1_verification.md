# 给另一台机器：A1 数据核实清单

> 我(这台机器的Claude)要把 `plan_2017_improvements.md` §1 表里列的 A1 资产写进
> report.md 的 §3/§7，但本机(+OneDrive+这个git仓库)都找不到 A1 的实际脚本/输出——
> 搜过 `*A1*`、`PortHills_dNBR`，都没有。plan 文档里的数字(242ha、dNBR=120.5、
> NBR 0.583→0.101)看起来是你核实过的，但我这边没法独立验证，写进report.md前
> 需要按 Yu 的规矩("每句话都要挂数据/文件/论文出处")核对一遍。

**麻烦回git仓库(或另开一份 `A1_verified_data.md`)把下面这些确认清楚：**

## 1. 智能掩膜（云 vs 云影 vs 烧痕）
- 脚本文件名+位置在哪（这台机器上找不到）？
- 具体逻辑：云(bit3)怎么掩、云影(bit4)为什么"只掩火场外、火场内保留"——判断
  依据是什么（比如亮度阈值、还是直接空间位置：火场边界内一律不当shadow掩掉）？
- **242 ha** 具体是什么的面积——"如果用naive掩膜会被错杀成shadow、但实际是
  烧痕"的像元总面积？还是别的定义？麻烦给出算这个数字的脚本片段或截图。

## 2. Offset 校正
- 控制环具体参数：内径500m/外径2km？怎么按elev/slope/覆盖类型匹配火场（用的
  哪个地形/土地覆盖数据源）？
- Dynamic World排除作物的具体逻辑（哪个class、阈值）？
- 那个"未烧植被dNBR本应=0却是120.5"的例子——这个点/像元在哪，offset校正前后
  的具体数值分别是多少？

## 3. 未烧岛 p90 阈值 + Otsu 被拒
- "未烧岛"是几个、手绘依据是什么（目视判读？还是有别的参考）？
- p90阈值算出来具体是多少(dNBR值)？
- Otsu试过之后"cut inside burned group"具体是什么意思——Otsu选出来的阈值把
  一群本该算"烧了"的像元切开了？能不能给一个对比图或数字？

## 4. 岛屿验证 + 未制图refugia发现
- recall / mean dNBR / covered ha 这三个数字分别是多少？
- "未制图refugia"具体多大面积、在哪个位置——是官方手绘图里漏掉的、我们的
  方法找到的未烧区？

## 5. 统计表 + 波段组合图
- pre/post/change 完整的那张统计表（所有波段，不只NBR）能不能直接给我原始
  数据（CSV或截图都行）？
- "NBR 0.583→0.101"这个数字——是全AOI平均，还是只算火场内？
- 自然色/CIR/SWIR烧痕组合、NDVI、BSI这几张图——是已经存在的png文件吗，在哪？

## 6. 2024 script 23（真彩+云影频率图）
- 这个脚本已经被移到A4仓库了，我这边拿不到。plan里说"照这个改"给2017用——
  能不能把这个脚本的核心逻辑（怎么算"云影频率"、怎么叠真彩）贴一份过来，或者
  你那边直接改好一版2017专用的丢进这个仓库？

---
不确定的部分你如果也没法当场核实（比如A1本来就是很早之前做的，细节记不清了），
照 Yu 的规矩说清楚"不确定"就行，别替我编数字——我们俩都不瞎猜。
