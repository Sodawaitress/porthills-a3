# 网站式报告 — 总结 + US设计

> 目标：把report.md里"提纲+中文写作提示"变成一个**读者一看就懂、图文出处清楚、
> 逻辑顺序讲故事**的网站，跟传统Word报告并存（这个网站补充，不替代——Learn交的
> 还是Word/PDF）。先设计section结构+每块的数据出处，你确认之后我再建。

---

## 一、这段时间做完的事 · 总结

### 2017主线（这次全力做的）
| 板块 | 核心发现 | 出处 |
|---|---|---|
| **智能云影掩膜** | 朴素掩膜会错杀242ha烧痕(误当云影) | 👤A1脚本代码注释，已验证 |
| **Offset季节漂移校正** | 未校正时未烧植被读数=120.5(该是0) | 🤖`26_output.txt`真实算出 |
| **未燃阈值** | 手绘岛p90=117.5，比Otsu(切错群)/2SD(148.5)更贴合，commission error仅5.8% | 🤖同上 |
| **岛屿独立验证** | 两块岛recall=0.851/0.953；反向"官方烧了→本方法也判烧了"=88.1% | 👤🤖CSV+脚本双重验证 |
| **⭐未制图refugia** | 本方法找到278.9ha未燃，官方地图只画了103.6ha在内——**175.3ha(63%)是官方完全没画出来的新发现refugia** | 🤖真实计算，本项目最重要的发现 |
| **Spearman vs Pearson** | `pre_B4`显著性会因方法而变(p=0.004→0.069) | 🤖`04e`，n=186 |
| **RBR vs dNBR** | RBR(SOTA方法)没有改善(R²=0.304 vs 0.322)，如实报告"测过没帮助" | 🤖`04f` |
| **SHAP变量重要性** | `slope`是pasture的隐藏头号特征(全局排名第7，分类别第1)；`pre_B4`对pasture强正向对其余负向 | 🤖`06d` |

### 2024副线（已验证但先shelve，等作后续/A4用）
5类燃料方案(gorse_broom/cleared_pine/pasture/broadleaf_scrub/exotic_pine)、CHM冠层高度当训练标签、Hansen森林损失法扩展到年份范围、LiDAR边缘效应+真实数据空洞两类踩坑、class-balanced空间分块——已移到独立仓库，见`methodology_playbook.md`留存的通用经验。

---

## 二、网站设计三条原则
1. **每一屏/每一块 = 一个US**，跟这个项目从头到尾的工作方式一致（设计→确认→做）。
2. **每张图/每个数字都挂来源角标**：📖外部文献 / 👤Yu的决定 / 🤖计算结果 / 🤝共同发现——
   跟`methodology_playbook.md`的出处纪律完全一致，网站上做成可点击的小标签，点开显示
   脚本名/文献链接，不是摆设。
3. **逻辑顺序=科学问题驱动，不是"章节堆栈"**：从"为什么要研究"→"怎么做的"→"发现了什么"
   →"这些发现说明什么"→"这个方法能力推广到哪"，读者跟着问题走，不是跟着rubric章节走
   （rubric顺序留给Word报告，网站可以按故事线重排）。

---

## 三、Section设计（WEB-US，一次一块，你确认顺序/取舍）

### WEB-US1 · Hero — 一句话钩子 + 全局地图
- **内容**：标题+一句话("2017 Port Hills大火后，哪里没烧？为什么？")+一张交互式地图
  (可切换：真彩/dNBR/分类/refugia四个图层)。
- **数据出处**：`PortHills2017_stack.tif`本地读取渲染，或用已导出的png做底图切换。
- **动画想法**：地图从"火前真彩"淡入到"dNBR热力图"，模拟"火烧过程"的视觉感。

### WEB-US2 · 为什么做这个研究（对应R2目的）
- 暖化→Canterbury火险→gorse清林遗产的因果链，做成一个小的时间轴/因果图动画。
- **出处**：WMO(B1)+Wyse(A3)+Meddens(A1)，References页可点开核实。

### WEB-US3 · 数据从哪来（对应R3数据探索，重点讲A1的技术活）
- **子块3a 智能掩膜**：并排对比"朴素掩膜会删掉什么" vs "智能掩膜保留了什么"——
  用`A1_visualisation_panels.png`+`A1_truecolor_burn_vs_shadow_2017.png`做成滑动对比
  (slider前后对比图，网页交互常见组件)。242ha这个数字做成一个突出的大字卡片。
- **子块3b Offset校正**：一个"校正前 vs 校正后"的dNBR分布直方图动画(120.5→0的漂移)。
- **子块3c 离群值/正态性发现**：boxplot+skew/kurt那组，做成"点哪个类看哪个类"的交互。
- **每块都配**：做了什么(一句)+为什么(一句)+限制(一句，小字)。

### WEB-US4 · 变量之间的关系（对应R4）
- 相关表做成热力图(不是文字表格)，Pearson/Spearman切换按钮(点一下看排序怎么变)。
- RBR vs dNBR并排回归结果卡片，标"测过，没有更好"的诚实结论。
- VIF清理过程做成"删一个变量、VIF怎么变"的分步动画。

### WEB-US5 · 分类结果（对应R5，视觉冲击力最强的一块）
- 4类分类地图 + 混淆矩阵(交互式，点格子看对应哪两类容易混)。
- SHAP那张图做成"选一个类，看哪个变量对它最重要+方向"的交互（比静态图更能体现
  "slope是pasture专属特征"这个发现）。
- gorse_broom/broadleaf_scrub混淆的JM证据配光谱曲线动画。

### WEB-US6 · ⭐ 核心发现：未制图的refugia（全站高潮，对应R7）
- 这是**整个项目最有冲击力的发现**，值得单独一屏，不要跟R7其他内容混在一起。
- **可视化想法**：官方两块岛(蓝色，114.9ha) vs 本方法找到的全部未燃区(绿色，278.9ha)
  叠加地图，用户能直接"看见"175.3ha的差异区域(用不同颜色高亮"新发现"部分)。
  配一个动态数字("278.9ha"从0滚动计数到目标值，62.9%用进度条视觉化)。
- 岛屿验证的两个recall数字(85.1%/95.3%)做成"证据可信度"的小徽章。

### WEB-US7 · 这套方法能力有多远（对应R6 way forward + 2024验证经验一句话带过）
- 流程图动画版(节点逐个亮起，配"这一步在解决什么问题")。
- 一句话点出"这套方法已经在另一场独立的2024火上复现验证过"，链接/引用
  `methodology_playbook.md`（不展开，这次网站主角是2017）。

### WEB-US8 · References（全站的"出处"页）
- 所有📖标签点开都跳到这里，按A3/A4/A5/A6分组(跟references.md一致)，可点DOI链接。
- 这页保证"科学严谨"这个要求——每句话都能查到出处，不是自说自话。

---

## 四、你需要决定的
1. **顺序**：上面WEB-US1-8是我建议的故事线，你要不要调整（比如WEB-US6提到最前面当
   开场冲击力，还是留在后面当"揭晓"）？
2. **深度**：8个板块全做，还是先做WEB-US1/3/6(最有冲击力的)出一个MVP，其余后补？
3. **技术形式**：单页scrollytelling(往下滑动展开全部内容) vs 多页(每个US一个页面
   +导航)？动画用CSS过渡够不够，还是要更重的(D3.js地图/图表交互)？

确认这份设计之后我用Artifact工具建，会先跑一次quickstart选合适的起始模板。
